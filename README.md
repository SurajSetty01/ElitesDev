# Project TODOs Before Deployment

1. Add CAPTCHA system to all forms.
2. Redesign the booking form with the new required options and Update database fields / entries based on the new booking form options.
3. After updating the booking form, copy the updated version into `services.html` after listing all services.
4. Update all content and images across the site.
5. Check and update all meta tags for proper SEO.
6. Check full mobile responsiveness across all pages; verify elements, layout, and CSS behave correctly on different screen sizes.
